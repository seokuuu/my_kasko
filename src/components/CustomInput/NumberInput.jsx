import { BlueInput } from '../../modal/Common/Common.Styled'

const NumberInput = (value, onChange, placeholder) => {
	return <BlueInput value={value} onChange={onChange} placeholder={placeholder} />
}

export default NumberInput
